{
  "name": "immich-people-plus",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/content/index.ts --bundle --outfile=dist/content.js --format=iife --target=es2022 && esbuild src/options.ts --bundle --outfile=dist/options.js --format=iife --target=es2022 && cp manifest.json dist/ && cp src/content.css dist/ && cp src/options.html dist/",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/justified-layout": "^4.1.4",
    "esbuild": "^0.25.0",
    "justified-layout": "^4.1.0",
    "typescript": "^5.8.2"
  }
}
